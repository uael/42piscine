module Board = struct
  type t = Player.m list

  (* return a new board *)
  let toggle x y p = function
  | [] ->
  | b ->

  let toString b =
end

type t = (Board.t list * (Player.t * Player.t))

(* return a new game *)
let toggle x y p = function
| [] ->
| b ->
